Thanks this [repo](https://github.com/deyuhua/dot-emacs-config), I refer this one to create my own emacs config.

Just clone this repo into ~/.emacs.d.
**Need to install corresponding lsp server** if need;
